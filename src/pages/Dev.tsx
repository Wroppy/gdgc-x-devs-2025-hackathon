import React from "react";
import LogInForm from "../components/login-form/LogInForm";

type Props = {};

const Dev = (props: Props) => {
  return (
    <div>
      <LogInForm />
    </div>
  );
};

export default Dev;
